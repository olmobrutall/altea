{
  // DATA layer — isomorphic model (shared client + server). Boilerplate in ../altea/presets/data.json.
  "extends": "../altea/presets/data.json",
  "references": [{ "path": "../altea/tsconfig.data.json" }]
}
