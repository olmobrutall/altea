{
  // Solution file — builds the three layers (`tspc -b`). Mirrors altea-email.
  "files": [],
  "references": [
    { "path": "./tsconfig.data.json" },
    { "path": "./tsconfig.server.json" },
    { "path": "./tsconfig.client.json" }
  ]
}
