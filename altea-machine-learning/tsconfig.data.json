{
  // DATA layer — the predictor definition (its main query, sub-queries and columns), the neural-network
  // settings, the codification / progress / result rows, the symbols and the PredictRequest model.
  "extends": "../altea/presets/data.json",
  "references": [
    { "path": "../altea/tsconfig.data.json" },
    { "path": "../altea-auth/tsconfig.data.json" },
    { "path": "../altea-files/tsconfig.data.json" },
    { "path": "../altea-processes/tsconfig.data.json" },
    { "path": "../altea-user-assets/tsconfig.data.json" }
  ]
}
