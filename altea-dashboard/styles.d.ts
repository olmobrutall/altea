// Ambient declaration for CSS side-effect imports (`import "./Dashboard.css"`). Mirrors altea-chart's
// styles.d.ts; picked up by the client tsconfig preset's `${configDir}/*.d.ts` include.
declare module "*.css";
