{
  // SERVER layer (engine: connection, linq, schema, sync, dynamicQuery, logic). node-only; references DATA.
  "extends": "./presets/server.json",
  "references": [{ "path": "./tsconfig.data.json" }]
}
