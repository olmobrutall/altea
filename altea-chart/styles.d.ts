// Ambient declaration for CSS side-effect imports (`import "./Chart.css"`). Mirrors eastwind/styles.d.ts;
// picked up by the client tsconfig preset's `${configDir}/*.d.ts` include.
declare module "*.css";
