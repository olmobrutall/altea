{
  // Solution file — builds the three suffix/directory-driven layers (`tspc -b`). Mirrors altea-dashboard.
  "files": [],
  "references": [
    { "path": "./tsconfig.data.json" },
    { "path": "./tsconfig.server.json" },
    { "path": "./tsconfig.client.json" }
  ]
}
