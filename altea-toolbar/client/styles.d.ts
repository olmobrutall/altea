// Ambient declaration for CSS side-effect imports (`import "./Toolbar.css"`). Mirrors altea-omnibox's
// client/styles.d.ts; picked up by the client tsconfig preset's include.
declare module "*.css";
