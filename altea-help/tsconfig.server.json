{
  // SERVER layer — the includes/operations, the prose generator, the per-culture caches, the search, the
  // inline-image extraction, the ZIP export/import and the routes.
  "extends": "../altea/presets/server.json",
  "references": [
    { "path": "./tsconfig.data.json" },
    { "path": "../altea/tsconfig.server.json" },
    { "path": "../altea-auth/tsconfig.server.json" },
    { "path": "../altea-files/tsconfig.server.json" },
    { "path": "../altea-omnibox/tsconfig.server.json" }
  ]
}
